[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "devlibs"
version = "1.0.1"
authors = [
  { name="Md Rifat Rahman" }
]
description = "Simple devlibs package"
readme = "README.md"
requires-python = ">=3.8"